#!/bin/bash

# Define the path to the Python script
PYTHON_SCRIPT_PATH="Python/SVG-Geometry.py"

# Run the Python script
python "$PYTHON_SCRIPT_PATH"

read -p "Press Enter to close the window..."